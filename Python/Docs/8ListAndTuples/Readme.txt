[]=>List
()=>Tuples
The main difference between a tuple and a list in Python is that a tuple is immutable, meaning its contents cannot be changed after creation, while a list is mutable and can be modified